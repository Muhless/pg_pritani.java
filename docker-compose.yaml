services:
  db:
    image: postgres:16-alpine
    container_name: pg_pritani_java
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: MUHLESS717GG
      POSTGRES_DB: pg_pritani_java
    ports:
      - "5435:5432"
    volumes:
      - demo_pgdata:/var/lib/postgresql/data

volumes:
  demo_pgdata:
